<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista formulario configuración documentos -->
    <record id="view_ds_config_form" model="ir.ui.view">
        <field name="name">ds.config.form</field>
        <field name="model">ds.config</field>
        <field name="arch" type="xml">
            <form string="Configuración de Documentos del Sistema">
                <sheet>
                    <group>
                        <field name="name" readonly="1"/>
                    </group>
                    <group>
                        <field name="footer_html" widget="html"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción -->
    <record id="action_ds_config" model="ir.actions.act_window">
        <field name="name">Configuración de Documentos</field>
        <field name="res_model">ds.config</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_ds_config_form"/>
        <field name="target">current</field>
    </record>

</odoo>


