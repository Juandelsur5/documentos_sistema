<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Reporte Documento del Sistema - Cotización / Orden -->
    <record id="action_report_ds_sale_order" model="ir.actions.report">
        <field name="name">Documento del Sistema (Cotización / Orden)</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">documentos_sistema.ds_sale_order_document</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
        <field name="print_report_name">'Documento del Sistema - %s' % (object.name)</field>
    </record>

</odoo>

